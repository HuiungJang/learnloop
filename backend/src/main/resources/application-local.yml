spring:
  datasource:
    url: ${APP_DATABASE_URL:jdbc:postgresql://localhost:5432/aicodelearning}
    username: ${APP_DATABASE_USERNAME:aicodelearning}
    password: ${APP_DATABASE_PASSWORD:aicodelearning}
  flyway:
    clean-disabled: false

logging:
  level:
    com.aicodelearning: INFO
